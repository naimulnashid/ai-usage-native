<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net10.0-windows10.0.26100.0</TargetFramework>
    <TargetPlatformMinVersion>10.0.19041.0</TargetPlatformMinVersion>
    <RootNamespace>UsageApp</RootNamespace>
    <AssemblyName>AIUsage</AssemblyName>
    <UseWinUI>true</UseWinUI>
    <Platforms>x64</Platforms>
    <Platform>x64</Platform>
    <RuntimeIdentifier>win-x64</RuntimeIdentifier>
    <!-- Unpackaged and self-contained: no MSIX, no certificate, and no
         dependency on whichever Windows App Runtime happens to be installed. -->
    <WindowsPackageType>None</WindowsPackageType>
    <WindowsAppSDKSelfContained>true</WindowsAppSDKSelfContained>
    <SelfContained>true</SelfContained>
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <ApplicationIcon>Assets\app.ico</ApplicationIcon>
    <!-- Program.cs is the entry point: it enforces a single instance. -->
    <DefineConstants>$(DefineConstants);DISABLE_XAML_GENERATED_MAIN</DefineConstants>
  </PropertyGroup>

  <ItemGroup>
    <!-- The components, not the Microsoft.WindowsAppSDK meta-package: that also
         pulls in the AI, ML and Search components (onnxruntime, DirectML), which
         this app never calls and which more than doubled the build. -->
    <PackageReference Include="Microsoft.WindowsAppSDK.WinUI" Version="2.3.9" />
    <PackageReference Include="Microsoft.WindowsAppSDK.Foundation" Version="2.3.12" />
    <PackageReference Include="Microsoft.WindowsAppSDK.InteractiveExperiences" Version="2.1.9" />
    <PackageReference Include="Microsoft.WindowsAppSDK.DWrite" Version="2.1.0" />
    <!-- The tray icon. The core package, not H.NotifyIcon.WinUI: that one
         depends on the 1.x Windows App SDK meta-package, which conflicts with
         the 2.x components above. -->
    <PackageReference Include="H.NotifyIcon" Version="2.4.1" />
    <!-- Every SVG (project logos, agent marks, the app icon) is drawn through
         Svg.Skia rather than WinUI's SvgImageSource, which supports only a
         subset of SVG and fails silently on the rest. -->
    <PackageReference Include="Svg.Skia" Version="5.2.3" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\UsageCore\UsageCore.csproj" />
  </ItemGroup>

  <ItemGroup>
    <Content Include="Assets\**\*" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>
</Project>

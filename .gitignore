# Build output
bin/
obj/
artifacts/
publish/
*.user
.vs/

# Transcript-derived data must never be committed: transcripts hold real code
# and paths from other projects. Tests build their fixtures at run time.
*.jsonl
demo-data/
out/

# Screenshots taken while developing can show real usage. Anchored: the
# README's own, made from invented data by tools\Capture-Readme.ps1, live in
# docs/screenshots/.
/screenshots/
